version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: resume-analyzer-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      MONGO_INITDB_DATABASE: ai-resume-analyzer

volumes:
  mongo-data:
